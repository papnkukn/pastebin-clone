<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/bootstrap.min.css" />
  <title>Pastebin</title>
</head>
<body style="background: #eee;">
  <div class="container-fluid mt-3">
  
    <div class="row">
      <div class="col-12">
        <h3 class="text-monospace">Pastebin <span class="text-primary text-monospace">list</span></h3>
        <div class="ml-auto">
          <a href="/" class="btn btn-sm btn-outline-primary" accesskey="q">New</a>
        </div>
      </div>
      <div class="col-12">
        <table class="table table-condensed" style="background: #fff;">
          <% for (let item of pages) { %>
          <tr>
            <td class="w-50 text-monospace" style="vertical-align: middle;"><%= item.id %></td>
            <td class="w-25 text-monospace" style="vertical-align: middle;"><%= item.created ? item.created.toString().replace("T", " ").replace("Z", "") : "--" %></td>
            <td class="w-25 text-right">
              <% if (typeof item.filename == "string" && item.filename) { %><a href="/<%= item.id %>/download" class="btn btn-sm btn-outline-secondary">Download</a><% } %>
              <!--<a href="/<%= item.id %>/delete" class="btn btn-sm btn-danger">Delete</a>-->
              <a href="/<%= item.id %>" class="btn btn-sm btn-primary">Edit</a>
            </td>
          </tr>
          <% } %>
        </table>
      </div>
    </div>
    
  </div>
</body>
</html>